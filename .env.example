# Copy to .env.local and fill in from your Supabase project (Settings → API).
# Both values are safe to expose in a static site — Row-Level Security is what
# protects the data. Leave them blank to keep PIA fully local (guest mode).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
